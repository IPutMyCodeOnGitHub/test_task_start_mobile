
jms_serializer:
    property_naming:
        separator:  _
        lower_case: true
    metadata:
        cache: file
        debug: "%kernel.debug%"
        file_cache:
            dir: "%kernel.cache_dir%/serializer"
        directories:
            App:
                namespace_prefix: App\Entity
                path: "%kernel.project_dir%/config/packages/serializer"